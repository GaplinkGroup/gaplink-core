[Unit]
Description=ntpclient
After=network.target

[Service]
Type=oneshot
#ExecStartPre=/usr/bin/sleep 30
ExecStart=/usr/bin/timeout 10 /usr/local/bin/ntpclient -h 0.arch.pool.ntp.org -d -s

[Install]
WantedBy=multi-user.target